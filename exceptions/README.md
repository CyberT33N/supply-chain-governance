# Exceptions

Top-level, cross-cutting exception records that are not bound to a single
dependency ecosystem live here. Each exception is a versioned, time-bounded,
reviewable governance decision.

An exception record never contains credentials, tokens, private keys, or
authorization headers. Expired exceptions authorize nothing.
